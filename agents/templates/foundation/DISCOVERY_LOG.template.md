# Discovery Log — `<Project Name>`

> Generated by Agent 26. **Append-only.** Records every discovery round, every question asked, every answer received, and every assumption inserted into the system.
> Purpose: auditability. The Meta-Agent Auditor (Agent 99) reviews this log to detect drift, hallucination, or premature commitment.

---

## Round 1 — `<YYYY-MM-DD HH:mm>` — Mode: `<LIGHT | AUTO | FULL>`

### Trigger
`<New project / context drift detected / user reopened discovery / orchestrator routed here>`

### Inputs available
- `<...>`

### Questions asked
1. `<question>` → user answer: `<...>` → recorded as `<CF-N | ASM-N>`
2. `<...>`

### Inferences made
- `<...>` → recorded as `IN-N`

### Assumptions inserted
- `<...>` → recorded as `ASM-N`

### Risks surfaced
- `<...>` → recorded in `RISKS_AND_ASSUMPTIONS.md`

### Complexity alerts
- `<...>`

### Outputs produced this round
- `<list of files created or updated>`

### Handoff status
- `<ready | partial | blocked>`
- Recommended next step: `<command or downstream agent>`

---

## Round 2 — `<...>`
`<...>`

---

## Conventions
- Every round includes: trigger, inputs, questions, inferences, assumptions, risks, alerts, outputs, handoff status
- No round may close without updating `CONTEXT_REGISTRY.md`
- No round may close while a blocking open question is unanswered AND no fallback assumption was recorded
